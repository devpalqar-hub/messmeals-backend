subsciptoin create akumga mess id ondakanam athil
deliveries create akumbo messId pass cheyannam
deliveries generate cheyyan query ayt messId pass cheyynam


1. lets add two field is paused and pause ending date and pause start date. 
the subscription plan will be saved as is_paused is true and pause ending date with the end date of pause 
and start date with start date of pause. plan should be paused before 1 day.
(pause start date and the date of pausing should have difference of atleast 1 days.)

2. add cancelled boolean field, cancellation start date and end date, 
3. generate usersubscriptions model by counting dates between start date and end date.


conflicts
1. as if deliveries are already generated before. pausing a plan will cause confusion. 
2. no conflict for cancellation


change will be on:
customer service
delivery service

  EVERYDAY
  CUSTOM

if scheduletype is everyday
    take start date and enddate 
        create deliveries isntance between start date and end date 
    
else scheduleType is custom 
    take start date and end date 
        calculate dates of selected days. 
        create deliveries instance for that selected days.

ensure both pause start date and pause end date is there. the pause end date will be less or equal to the subsciptoin end date as user should not pause for non subscription deliveryPartnerProfileId
 - count no of days between pause start date and pause end date. take deliveries instance of subscriptionId with pause_start_date, from that deliveries
 instance to the rest of the deliveries instance of that subsciptionId, change the date starting with pause end date to date of each day for rest of the 
deiveris instance.
    

current date should be 2 days before pause date. 