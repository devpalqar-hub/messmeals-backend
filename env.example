DATABASE_URL=""

# Mapbox Geocoding API token, used by GeocodingService for the location half of
# GET /open/search-suggestions. Without it, that endpoint's `locations` list is always empty.
MAPBOX_ACCESS_TOKEN=""
